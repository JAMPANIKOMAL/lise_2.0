name: Web Injection Scenario
description: A test scenario for Phase 2 Docker integration.
teams:
  blue:
    name: Blue Team
    dockerfile: ./Dockerfile.test
  red:
    name: Red Team
    dockerfile: ./Dockerfile.test